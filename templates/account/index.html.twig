{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}

    <div class="container">
        <ul>
            {% for account in accounts %}
                <li>{{ 'account.index.table.name'|trans }}</li>
                <li>{{ account.name }}</li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}
